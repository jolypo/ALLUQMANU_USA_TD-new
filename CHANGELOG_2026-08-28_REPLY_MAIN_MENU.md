# Telegram Main Menu — ReplyKeyboard update

- Changed only the top-level private admin menu to `ReplyKeyboardMarkup`.
- The main keyboard is persistent and resizes to the Telegram client.
- Nested menus remain `InlineKeyboardMarkup`.
- Candidate selection and `Approve / Cancel` remain inline callbacks.
- Existing slash commands remain available as fallback.
- No trading strategy, SPX V20/Core logic, risk logic, reports, or trade lifecycle behavior was changed.
